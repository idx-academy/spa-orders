import { createTheme } from "@mui/material/styles";

//There will be used exported colours from '/palette.scss'

export const theme = createTheme({
  palette: {
    primary: {
      main: "#000000",
    },
    secondary: {
      main: "#751FFF",
    },
  },
});
