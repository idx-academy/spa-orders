import InputBase from "@mui/material/InputBase";

import { AppInputBaseProps } from "@/components/app-input-base/AppInputBase.types";

const AppInputBase = (props: AppInputBaseProps) => {
  return <InputBase {...props} />;
};

export default AppInputBase;
