import FormHelperText, {
  FormHelperTextProps
} from "@mui/material/FormHelperText";

const AppFormHelperText = (props: FormHelperTextProps) => {
  return <FormHelperText {...props} />;
};

export default AppFormHelperText;
